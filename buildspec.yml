version: 0.1

phases: 
 build:
  commands:
   - echo Entered the build phase...
   - mvn clean package
 post_build:
  commands:
  - echo build completed successfully...
  
artifacts:
 files:target/docker-spring-boot.jar
  discard-paths: yes